# MambaX Backend - Vercel Serverless (минимальный размер)
# =============================================
# Лимит Vercel: 250 MB unzipped
# =============================================

# Core
fastapi==0.109.0
uvicorn==0.27.0

# Database (Neon PostgreSQL) - только asyncpg
sqlalchemy==2.0.25
asyncpg==0.29.0

# Validation & Settings
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator
python-dotenv==1.0.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP
httpx==0.27.0
aiohttp>=3.9.0

# Redis
redis>=5.0.0

# Utils
python-dateutil>=2.8.2
apscheduler>=3.10.0

# Monitoring (optional)
sentry-sdk[fastapi]>=1.39.0
