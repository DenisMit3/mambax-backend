# =============================================
# MAMBAX FRONTEND - EXAMPLE CONFIGURATION
# =============================================

# Backend URL (Render)
BACKEND_URL=https://mambax-api.onrender.com

# API Configuration (for client-side, uses proxy)
NEXT_PUBLIC_API_URL=/api_proxy

# WebSocket URL (direct to backend — Vercel rewrites don't proxy WS)
NEXT_PUBLIC_WS_URL=wss://mambax-api.onrender.com/api/chat/ws

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Telegram
NEXT_PUBLIC_TELEGRAM_BOT_NAME=YouMeMeet_bot
