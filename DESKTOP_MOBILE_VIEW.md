# 📱 Руководство по Мобильному Виду на Десктопе

Приложение MambaX разработано с подходом **Mobile-First**. Для удобной разработки и тестирования на компьютере реализована специальная эмуляция мобильного устройства.

## 🖥 Как это выглядит?

При открытии приложения на широком экране (более `768px`) интерфейс автоматически:
1. Центрируется на экране.
2. Ограничивается шириной `480px` (стандартный размер мобильного устройства).
3. Добавляет темный фон вокруг "телефона" для фокуса внимания.
4. Добавляет тени и границы для визуального выделения.

Благодаря этому вы видите приложение так же, как пользователь на реальном смартфоне, без необходимости постоянного изменения размера окна браузера.

## 🛠 Техническая реализация

В файле `frontend/src/app/globals.css` добавлены медиа-запросы:

```css
@media (min-width: 768px) {
  /* Обертка для десктопа */
  .desktop-wrapper {
    display: flex;
    justify-content: center;
    background-color: #1a1a1a; /* Темный фон */
    min-height: 100vh;
  }

  /* Рамка "телефона" */
  .mobile-frame {
    width: 100%;
    max-width: 480px;
    background: var(--background);
    box-shadow: 0 0 50px rgba(0, 0, 0, 0.5); /* Тень */
  }
}
```

В файле `frontend/src/app/layout.tsx` структура приложения обернута в соответствующие контейнеры:

```tsx
<div className="desktop-wrapper">
  <div className="mobile-frame">
    <ClientLayout>{children}</ClientLayout>
  </div>
</div>
```

## 🔍 Инструменты разработчика (DevTools)

Хотя встроенная эмуляция удобна, для проверки точного поведения на разных устройствах (iPhone SE, Pixel, iPhone 14 Pro Max) рекомендуется использовать **Chrome DevTools**:

1. Нажмите `F12` (или `Ctrl + Shift + I`).
2. Нажмите иконку 📱 "Toggle device toolbar" (или `Ctrl + Shift + M`).
3. Выберите нужное устройство из списка вверху.

## 🚀 Тестирование на реальном устройстве

Для максимальной точности тестируйте на реальном телефоне через локальную сеть:
1. Запустите `start_local_network.bat` (Windows) или `./start_local_network.sh` (Mac/Linux).
2. Отсканируйте **QR-код** в консоли камерой телефона.
3. Приложение откроется в браузере вашего смартфона.
