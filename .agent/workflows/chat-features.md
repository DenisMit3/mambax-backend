---
description: Как работать с чат-функциями (кнопки, emoji, голосовые)
---

# Chat Features — MambaX

## Расположение кода
- **Чат страница**: `frontend/src/app/chat/[id]/page.tsx`
- **Глобальные стили**: `frontend/src/app/globals.css`

## Реализованные функции

### 1. Кнопка изображения (📷)
- Использует `<label>` с `<input type="file">` внутри
- `accept="image/*"` для выбора изображений
- `capture="environment"` для камеры на мобильных

### 2. Кнопка микрофона (🎤)
- Запись через `MediaRecorder` API
- Проверка поддержки `navigator.mediaDevices.getUserMedia`
- Audio constraints: echoCancellation, noiseSuppression

### 3. Emoji Picker (😊)
**Расположение:** Внутри input area, слева от текстового поля

**Категории:**
- 🔥 Популярные
- 😀 Смайлы  
- ❤️ Любовь
- 👋 Жесты
- 👤 Люди
- 🐶 Животные
- 🍕 Еда
- ⚽ Активности
- 🌍 Путешествия
- 🌸 Природа
- 🎁 Объекты
- 💯 Символы
- 🏳️ Флаги

**Особенности:**
- Секция "Недавние" — последние 8 использованных эмодзи
- Адаптивная сетка: `repeat(auto-fill, minmax(40px, 1fr))`
- max-width: 400px для центрирования
- safe-area-inset для iPhone notch

## CSS Анимации

```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

## Мобильная адаптивность

Обязательные стили для интерактивных элементов:
```tsx
style={{
    WebkitTapHighlightColor: 'transparent',
    touchAction: 'manipulation',
    pointerEvents: 'auto',
    minWidth: '44px',
    minHeight: '44px'
}}
```

CSS override для глобального `user-select: none`:
```css
button, input, label, a {
  pointer-events: auto;
  touch-action: manipulation;
}
```
