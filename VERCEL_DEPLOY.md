# 🚀 Деплой MambaX на Vercel - Пошаговая инструкция

## Что деплоим на Vercel

**Frontend (Next.js)** - идеально подходит для Vercel:
- ✅ Бесплатный хостинг
- ✅ Автоматический SSL
- ✅ CDN по всему миру
- ✅ Автодеплой из GitHub

**Backend** - нужен отдельный хостинг (см. ниже)

---

## Шаг 1: Подготовка репозитория

### 1.1 Убедитесь что код в GitHub

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push origin main
```

---

## Шаг 2: Деплой Frontend на Vercel

### 2.1 Регистрация на Vercel

1. Перейдите на https://vercel.com
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"**
4. Авторизуйте Vercel для доступа к репозиториям

### 2.2 Импорт проекта

1. На Dashboard нажмите **"Add New..." → "Project"**
2. Найдите репозиторий `sait-znakomstv` (или как он называется)
3. Нажмите **"Import"**

### 2.3 Настройка проекта

В настройках укажите:

| Параметр | Значение |
|----------|----------|
| **Framework Preset** | Next.js |
| **Root Directory** | `frontend` |
| **Build Command** | `npm run build` |
| **Output Directory** | `.next` |
| **Install Command** | `npm install` |

### 2.4 Environment Variables

Нажмите **"Environment Variables"** и добавьте:

```
BACKEND_URL=https://ваш-бэкенд.com
NEXT_PUBLIC_API_URL=/api_proxy
NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app
NEXT_PUBLIC_TELEGRAM_BOT_NAME=YouMeMeet_bot
```

⚠️ **BACKEND_URL** - это адрес вашего бэкенда (см. Шаг 3)

### 2.5 Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения (2-5 минут)
3. Получите URL вида: `https://ваш-проект.vercel.app`

---

## Шаг 3: Бэкенд - Варианты хостинга

Vercel **НЕ подходит** для бэкенда из-за:
- WebSockets (чат)
- Фоновых задач (APScheduler)
- Долгих соединений с БД

### Вариант A: Railway (рекомендуется)

**Бесплатно**: $5 кредитов/месяц (хватает для небольшого проекта)

1. Зайдите на https://railway.app
2. "Start a New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий, укажите `backend` как Root Directory
4. Добавьте переменные окружения (DATABASE_URL, SECRET_KEY, etc.)

### Вариант B: Fly.io

**Бесплатно**: 3 маленьких VM

```bash
# Установите flyctl
curl -L https://fly.io/install.sh | sh

# Авторизация
fly auth login

# Деплой
cd backend
fly launch
fly deploy
```

### Вариант C: Локальный сервер + ngrok

Для тестирования:

```bash
# Запустите бэкенд локально
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000

# В другом терминале - туннель
ngrok http 8000
```

Используйте ngrok URL как BACKEND_URL в Vercel.

---

## Шаг 4: Связываем Frontend и Backend

После деплоя бэкенда:

1. Скопируйте URL бэкенда (например `https://mambax-api.up.railway.app`)
2. В Vercel Dashboard → Settings → Environment Variables
3. Обновите `BACKEND_URL` на новый адрес
4. Нажмите **"Redeploy"** (три точки → Redeploy)

---

## Шаг 5: Настройка домена (опционально)

### Свой домен

1. Vercel Dashboard → Settings → Domains
2. Добавьте домен
3. Настройте DNS записи у регистратора

### Бесплатный поддомен Vercel

Ваш проект автоматически получает:
- `https://ваш-проект.vercel.app`

---

## Шаг 6: Telegram Mini App

Обновите URL в BotFather:

```
/mybots → @YouMeMeet_bot → Bot Settings → Menu Button
URL: https://ваш-проект.vercel.app
```

---

## Проверка деплоя

После деплоя проверьте:

1. **Frontend**: `https://ваш-проект.vercel.app` - должна открыться страница
2. **API Proxy**: `https://ваш-проект.vercel.app/api_proxy/health` - должен вернуть `{"status": "healthy"}`
3. **Telegram**: Откройте бота и нажмите кнопку меню

---

## Troubleshooting

### Ошибка "Build failed"

```bash
# Проверьте локально
cd frontend
npm install
npm run build
```

### Ошибка "API not reachable"

1. Проверьте что бэкенд запущен
2. Проверьте BACKEND_URL в Environment Variables
3. Проверьте CORS настройки бэкенда

### Ошибка "WebSocket connection failed"

WebSocket через Vercel rewrites может не работать.
Используйте прямое подключение к бэкенду:

```typescript
// В frontend коде
const WS_URL = process.env.NEXT_PUBLIC_WS_URL || 'wss://ваш-бэкенд.com';
```

---

## Стоимость

| Сервис | Бесплатный лимит |
|--------|------------------|
| **Vercel** (Frontend) | 100GB bandwidth, unlimited deploys |
| **Railway** (Backend) | $5/месяц кредитов |
| **Neon** (PostgreSQL) | 0.5GB storage, 190 compute hours |
| **Upstash** (Redis) | 10K commands/day |

**Итого**: Полностью бесплатно для небольшого проекта!
